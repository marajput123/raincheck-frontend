import { SxProps } from "@mui/system";
import { stylePanelBoxShadow } from "src/Shared/Contants";

export const panelStyle: SxProps = {
  border: "1px solid #EAEAEA",
  boxShadow: stylePanelBoxShadow,
  borderRadius: "30px",
}
